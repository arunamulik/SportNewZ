<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>P_Name__c</fullName>
    <externalId>false</externalId>
    <formula>if(RecordType.Name = &quot;Partner Plan&quot;,

Partner__r.Name &amp; &quot; - &quot; &amp; 

if(year(Start_Date__c) = year(End_Date__c), text(year(Start_Date__c)), text(year(Start_Date__c)) &amp; &quot;/&quot; &amp; right(text(year(End_Date__c)),2)),

Project_Name__c &amp; &quot; - &quot; &amp; 

if(year(Start_Date__c) = year(End_Date__c), text(year(Start_Date__c)), text(year(Start_Date__c)) &amp; &quot;/&quot; &amp; right(text(year(End_Date__c)),2)))</formula>
    <label>Name</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
